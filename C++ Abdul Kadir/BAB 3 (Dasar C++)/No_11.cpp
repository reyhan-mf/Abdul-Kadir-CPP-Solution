#include <iostream>
#include <cmath>

using namespace std;

int main()
{
}

// (a)
// double y, x;
// cout << "Masukkan x: ";
// cin >> x;
// y = 5 * (pow(x, x)) - 6 * x + 7;
// cout << y;

// (b)
// double a, b, c;
// cout << "Masukkan b: ";
// cin >> b;
// cout << "Masukkan c: ";
// cin >> c;
// a = (b + 2) / (1 + (c - 2));
// cout << a;

// (c)
// double x, y;
// cout << "Masukkan x: ";
// cin >> x;
// y = (pow(x, 5) + 6) / (3 * (pow(x, 2)));
// cout << y;

// (d)
// double x, y;
// cout << "Masukkan x: ";
// cin >> x;
// y = (1.0 / 3) * pow(x, 2) + 6 * x - 5;
// cout << y;